# 版本记录

## 1.1.2（开发版）

- 新增行内字典：行内处室、打卡地点、行内项目（项目挂处室下）；系统管理新增「字段维护」页，处室/项目按左侧处室栏 + 右侧项目列表联动浏览，字典被用户引用时禁止删除
- 个人信息：行内处室/行内项目（先选处室再选项目）/打卡地点改为下拉选择；对应审批人、工作代理人改为「先按处室筛选、再选具体人员」的分级选择（多列选择器）
- 行内级别默认分配「1级」，用户不可修改（点击提示联系管理员），仅超级管理员可在用户管理中调整
- 用户管理刷新排序修复：全站列表查询补唯一决胜键（id），消除同秒/同名数据刷新后顺序漂移
- 加班登记取消开始/结束时间，改按下班时间 + 加班时长（2 至 6 个整小时，实际加班时间以此为准），打卡提醒同步新口径
- 请假单下载前增加确认框，提示下载文件名（用户名_日期_请假单）
- 修复：字段维护图标白块（坏图检测补全图校验并重新生成）、系统 toast 文案截断（图标提示限 7 字、长文本改弹窗）、PC 端返回按钮垂直居中对齐

## 1.1.0（开发版）

- 审批超时催办：待审批超 24 小时推送审批管理员，未处理每 24 小时再提醒
- 审批页：通过/驳回防重复点击；卡片可点开详情弹层（含提交时间/事由/代理人），可在弹层内直接审批；「最近通过」补空态文案
- 加班登记：开始/结束时间自选（可跨零点），时长按已满整小时向下取整记录（如 17:30–20:29 计 2 小时），不足 2 小时或超 6 小时拦截并即时提示
- 请假申请：周末与法定节假日日期不可选；选择假别后即时显示剩余年假/调休额度，超出可用额度置灰提交
- 快捷办理：输入内容与结果以对话气泡展示，不再强制填写请假事由；驳回原因占位文案修正；撤销加班命中单条时直接弹确认
- 二级页（加班/请假/审批/月度记录/用户管理）加载失败改为页内错误提示 + 重新加载；首页刷新失败保留旧数据并提示
- 个人信息：保存后局部更新避免整页闪烁；手机号/紧急联系人增加 11 位格式校验；未保存修改返回前二次确认
- 首页值班冲突提醒携带日期直达员工情况页并高亮该日期
- 清理遗留微信订阅消息表与 WECHAT_SUBSCRIBE 相关配置（数据库迁移 016/017/018）
- 其它：用户管理补加载/空态；月份记录加载失败可重试

## 1.0.7（开发版）

- 助手页去 AI 化改造为「快捷办理」指令页：移除聊天/语音对话等深度合成形态与 AI 文案，保留正则指令解析及请假/加班/查询/审批等全部办理能力
- 语音输入（同声传译，语音转文字）保留
- 新增闪电图标，AI 图标移出安装包

## 1.0.6（开发版）

- 加班登记后推送「工作日加班打卡提醒」（日期/时长/打卡要求/备注）
- 审批通知标题简短化：待审批「张三申请1天调休」、审批结果「你的调休申请已通过」
- 加班补录窗口改为三个月（与额度有效期一致，超过即过期）
- 恢复安全约束：绑定已有用户仅限普通用户账号，管理员/超管禁止绑定
- 隐藏 AI 深度问答开关与系统管理「AI 设置」入口，AI 助手仅保留正则规则匹配
- 系统管理入口卡片 button 改 view，修复 flex 换行后单卡被默认 auto 外边距居中、未与用户管理对齐
- 首页「系统管理」描述去除 AI 字样

## 1.0.5（开发版）

- 提审风险修复：sitemap 收紧管理页索引、打包排除运维文档、推送验证按钮去除「测试」字样
- 修复：AI 助手输入框发送后自动清空、流式降级旧基础库「网络异常」、请假日历「今天」高亮、提交后日期残留、导航组件补齐 home 方法
- UI 优化：onboard 次级按钮样式、duty/编辑用户弹层滚动兜底、微型文字放大、8 页底部安全区内边距、admin/ai 页滚动修复、员工情况头像首字居中

## 1.0.4（开发版）

- 请假申请改版：日期选择独立弹层，多天请假支持开始/结束时段选择（开始仅全天/下午，结束仅上午/全天），移除手动填写事由
- 注册/绑定手机号格式校验（前后端一致，11 位手机号）
- 注册引导合并「审批人 + 代理人」为一步，一次性设置

## 1.0.3（开发版）

- AI 助手语音按钮优化
- 用户注册引导新增「审批人」步骤
- 其余体验优化
- 个人信息页支持自行修改审批人

## 1.0.2

- 修复瞬时断网清空会话导致「尚未连接后端」
- 半天假限调休/哺乳假/公出
- 年假额度校验
- 修复考勤导出与节假日历图标
- 正式域名 HTTPS

## 1.0.1

- 新增年假额度校验
- 半天假限调休/哺乳假/公出
- 超管新增考勤导出与节假日历图标
- 值班/请假数据导出
- 接入 LLM
- 配置正式域名 HTTPS

## 1.0.0（首次上传）

- 值班/加班登记
- 请假申请与一级审批
- 调休余额与到期提醒
- 通讯录与员工情况查询
- AI 语音文字助手
- 审批签名与请假单下载
- wxpusher 审批推送