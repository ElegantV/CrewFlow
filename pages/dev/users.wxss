page { height: 100vh; display: flex; flex-direction: column; }
.scrollarea { flex: 1; width: 100%; height: 0; box-sizing: border-box; }
.warning { margin-bottom: 24rpx; padding: 20rpx 24rpx; border-radius: 16rpx; color: #92400e; background: #fffbeb; font-size: 24rpx; line-height: 1.5; }
.current { display: flex; flex-direction: column; padding: 28rpx; }
.current-label { color: #718096; font-size: 22rpx; }
.current-name { margin-top: 8rpx; font-size: 32rpx; font-weight: 700; }
.current-role { margin-top: 8rpx; color: #2563eb; font-size: 23rpx; }
.user-list { display: flex; flex-direction: column; margin-top: 24rpx; }
.user-card { display: flex; align-items: center; justify-content: space-between; margin-bottom: 18rpx; padding: 24rpx; }
.user-info { display: flex; flex: 1; flex-direction: column; min-width: 0; }
.user-name { font-size: 28rpx; font-weight: 700; }
.user-meta { margin-top: 7rpx; color: #718096; font-size: 22rpx; }
.switch-button { flex-shrink: 0; margin: 0 0 0 20rpx; padding: 0 24rpx; border-radius: 14rpx; color: #2563eb; background: #eff6ff; font-size: 24rpx; }
.switch-button::after { border: 0; }
.restore-button { margin-top: 32rpx; border-radius: 16rpx; color: #fff; background: #475569; }
.restore-button::after { border: 0; }
.loading { padding: 64rpx; color: #718096; text-align: center; }
