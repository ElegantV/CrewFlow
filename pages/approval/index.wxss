page { height: 100vh; display: flex; flex-direction: column; }
.scrollarea { flex: 1; width: 100%; height: 0; box-sizing: border-box; }
.approval-list { display: flex; flex-direction: column; }
.subscribe-tip { display: flex; align-items: center; margin-bottom: 24rpx; padding: 22rpx 24rpx; border: 1rpx solid #bfdbfe; border-radius: 18rpx; background: #eff6ff; }
.subscribe-body { display: flex; flex: 1; flex-direction: column; min-width: 0; }
.subscribe-title { color: #1e40af; font-size: 27rpx; font-weight: 700; }
.subscribe-text { margin-top: 6rpx; color: #1d4ed8; font-size: 22rpx; line-height: 1.5; }
.subscribe-button { flex: 0 0 auto; height: 60rpx; margin: 0 0 0 20rpx; padding: 0 24rpx; border-radius: 14rpx; color: #fff; background: #2563eb; font-size: 24rpx; line-height: 60rpx; }
.subscribe-button::after { border: 0; }
.approval-card { display: flex; flex-direction: column; margin-bottom: 20rpx; padding: 28rpx; }
.approval-head { display: flex; justify-content: space-between; align-items: center; }
.applicant { font-size: 30rpx; font-weight: 700; }
.type { padding: 6rpx 14rpx; border-radius: 999rpx; color: #2563eb; background: #eff6ff; font-size: 22rpx; }
.date { margin-top: 20rpx; font-size: 27rpx; }
.meta, .reason { margin-top: 10rpx; color: #718096; font-size: 23rpx; line-height: 1.5; }
.actions { display: flex; flex-direction: row; margin-top: 24rpx; }
.actions button { flex: 1; margin: 0; }
.actions button + button { margin-left: 20rpx; }
.reject-button, .approve-button { border-radius: 14rpx; font-size: 26rpx; }
.reject-button { color: #dc2626; background: #fef2f2; }
.approve-button { color: #fff; background: #059669; }
.reject-button::after, .approve-button::after { border: 0; }
.empty, .loading { padding: 80rpx 24rpx; color: #718096; text-align: center; }
.section-title { margin: 36rpx 0 18rpx; color: #475569; font-size: 26rpx; font-weight: 700; }
.history-list { margin-bottom: 24rpx; }
.type--approved { color: #047857; background: #ecfdf5; }
