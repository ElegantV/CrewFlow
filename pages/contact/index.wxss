page { height: 100vh; display: flex; flex-direction: column; background: var(--c-bg); }
.contact-layout { display: flex; flex: 1; min-height: 0; border-top: 1rpx solid #e7ebf0; }
.system-list { flex-shrink: 0; width: 184rpx; height: 100%; background: #fff; }
.system-item { position: relative; display: flex; min-height: 96rpx; align-items: center; padding: 18rpx 20rpx; color: #4b5565; font-size: 24rpx; font-weight: 600; box-sizing: border-box; transition: background-color .2s ease, color .2s ease; }
.system-item--active { color: var(--brand); background: var(--c-info-bg); }
.system-indicator { position: absolute; top: 24rpx; bottom: 24rpx; left: 0; width: 6rpx; border-radius: 0 var(--r-xs) var(--r-xs) 0; background: transparent; }
.system-item--active .system-indicator { background: var(--brand); }
.contact-panel { display: flex; flex: 1; min-width: 0; flex-direction: column; padding: 18rpx 16rpx; box-sizing: border-box; }
.panel-head { display: flex; flex-shrink: 0; align-items: center; justify-content: space-between; padding: 6rpx 4rpx 20rpx; }
.panel-title { color: #172033; font-size: 30rpx; font-weight: 700; }
.panel-subtitle { margin-left: 12rpx; color: #8a96a6; font-size: 21rpx; }
.panel-tip { color: #9aa5b3; font-size: 19rpx; }
.contact-list { flex: 1; width: 100%; height: 0; }
.contact-card { position: relative; width: 100%; min-height: 144rpx; margin-bottom: 14rpx; padding: 22rpx 154rpx 22rpx 20rpx; border: 1rpx solid #e8edf3; border-radius: var(--r-md); background: #fff; box-shadow: 0 5rpx 16rpx rgba(31, 35, 41, .035); box-sizing: border-box; transition: transform .12s ease, box-shadow .2s ease; }

.contact-card:active {
  transform: scale(.99);
}
.contact-main { display: flex; width: 100%; min-width: 0; flex-direction: column; }
.contact-name-row { display: flex; align-items: center; }
.contact-name { overflow: hidden; color: #172033; font-size: 27rpx; font-weight: 700; text-overflow: ellipsis; white-space: nowrap; }
.type-tag { flex-shrink: 0; margin-left: 9rpx; padding: 4rpx 9rpx; border-radius: var(--r-xs); color: var(--c-text-muted); background: var(--c-neutral-bg); font-size: 18rpx; }
.type-tag--bank { color: var(--c-info); background: var(--c-info-bg); }
.type-tag--digital { color: var(--c-success); background: var(--c-success-bg); }
.type-tag--vendor { color: #a16207; background: #fef3c7; }
.contact-system, .contact-mobile { overflow: hidden; margin-top: 8rpx; color: #748196; font-size: 21rpx; text-overflow: ellipsis; white-space: nowrap; }
.contact-mobile { color: #526173; }
.contact-actions { position: absolute; top: 50%; right: 10rpx; display: flex; width: 136rpx; transform: translateY(-50%); }
.icon-button { display: flex; width: 66rpx; height: 88rpx; flex-direction: column; align-items: center; justify-content: center; margin: 0; padding: 0; color: var(--brand); background: transparent; font-size: 17rpx; line-height: 1.2; }
.icon-button + .icon-button { margin-left: 4rpx; }
.icon-button::after { border: 0; }
.icon-button--disabled { color: #b7c0cc; opacity: .45; }
.icon-shell { display: flex; width: 50rpx; height: 50rpx; align-items: center; justify-content: center; margin-bottom: 7rpx; border-radius: 50%; background: var(--c-info-bg); }
.action-icon { width: 31rpx; height: 31rpx; }
.empty { padding: 100rpx 20rpx; color: #8a96a6; text-align: center; }
.page-state { display: flex; flex: 1; align-items: center; justify-content: center; color: var(--c-text-muted); font-size: 24rpx; }
.page-state--error { flex-direction: column; color: var(--c-danger); }
.page-state button { margin-top: 20rpx; color: var(--brand); background: var(--c-info-bg); font-size: 23rpx; }
.page-state button::after { border: 0; }
